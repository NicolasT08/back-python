version: '3.8'

services:

  mysql:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: ancianato_db
      MYSQL_USER: usuario
      MYSQL_PASSWORD: password